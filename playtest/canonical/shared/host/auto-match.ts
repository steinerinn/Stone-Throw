import {areaScoutChoice} from '../policy/area-scout.js';
import type {HostExecution} from './lifecycle.js';
import type {HostState} from './contracts.js';import {acceptCommand,pumpHost} from './lifecycle.js';import {seat,parseKey,cellKey} from '../combat/access.js';import {autoMatchTarget,scoutChoices} from '../policy/auto-target.js';import {heroRelocation,heroLocalEscape,catapultOrigin,bestCatapult} from '../policy/special-decisions.js';
/** Private simulation controller. Policy draws are recorded in the RNG journal;
 * accepted commands still pass the same host legality/decision interface. */
export function autoStep(input:HostState,execution?:HostExecution):HostState {let h=structuredClone(input);const commandId='auto-'+(h.history.length+1);if(h.status==='placement')return acceptCommand(h,{id:commandId,kind:'start'},execution);if(h.status==='awaiting-turn'){if(h.activePlayerId===h.config.players[1]!.id){h.guards.outerIterations++;if(h.guards.outerIterations>1200){h.status='guarded';h.guards.reason='outer-loop-limit';return h;}if(h.round>500){h.status='guarded';h.guards.reason='round-limit';return h;}}return acceptCommand(h,{id:commandId,kind:'advance-turn'},execution);}if(h.status==='awaiting-decision'){const root=h.pendingRoot!,d=root.decisions.find(d=>d.status==='pending')!,owner=d.actorId;let cell=null,unitId=null;if(d.kind==='hero-relocation'){const local=root.state.match.units.find(u=>u.id===d.unitId)?.hero?.hitsTaken===2;const chosen=local?heroLocalEscape(h,owner,d.legalCells.map(cellKey)):heroRelocation(h,owner);cell=chosen?parseKey(chosen):null;}else if(d.kind==='catapult-target'){const origins=root.decisions.filter(x=>x.kind==='catapult-target'&&x.workId===d.workId&&x.status==='answered'&&x.answer?.cell).map(x=>cellKey(x.answer!.cell!)),chosen=catapultOrigin(h,owner,[],origins);cell=chosen?parseKey(chosen):null;}else if(d.kind==='catapult-roll'){const chosen=bestCatapult(h,owner,d.legalCells.map(cellKey));cell=chosen?parseKey(chosen):null;}else if(d.kind==='scout'&&d.area){h.pendingPolicyCells=[];cell=areaScoutChoice(h,owner,d.boardId,d.legalCells);}else if(d.kind==='scout'){if(!h.pendingPolicyCells.length)h.pendingPolicyCells=scoutChoices(h,owner,d.remaining).map(parseKey);cell=h.pendingPolicyCells.shift()||(d.area?d.legalCells[0]||null:null);}else throw Error('Interactive-only or unsupported policy decision '+d.kind);h=acceptCommand(h,{id:commandId,kind:'answer',answer:{decisionId:d.id,actorId:owner,cell,unitId}},execution);if(h.guards.pendingPrimaryLimit&&!h.pendingRoot){h.guards.pendingPrimaryLimit=false;if(h.status!=='complete'){h.status='guarded';h.guards.reason='player-turn-limit';}}return h;}if(h.status==='awaiting-command'){const owner=h.activePlayerId!,p=seat(h.state,owner),chosen=autoMatchTarget(h,owner,p.sameTurnBonusTarget==='chain'?h.plagueAwareAtTurnStart:h.state.plagues.some(q=>q.targetPlayerId===p.reactionTarget.playerId));if(!chosen){if(p.sameTurnBonusTarget==='ordinary'){if(h.guards.turnActions>=1199){h.status='guarded';h.guards.reason='player-turn-limit';return h;}h.guards.turnActions=0;h.guards.outerIterations++;if(h.guards.outerIterations>1200){h.status='guarded';h.guards.reason='outer-loop-limit';}else if(h.round>500){h.status='guarded';h.guards.reason='round-limit';}}else{h.turnStep='plague';h.status='running';pumpHost(h,100000,execution);}return h;}const attempts=h.guards.turnActions+1,primary=p.sameTurnBonusTarget==='ordinary';h=acceptCommand(h,{id:commandId,kind:'shoot',actorId:owner,boardId:p.reactionTarget.boardId,cell:parseKey(chosen)},execution);if(primary&&attempts>=1200&&h.status!=='complete'){if(h.pendingRoot)h.guards.pendingPrimaryLimit=true;else{h.status='guarded';h.guards.reason='player-turn-limit';}}return h;}return h;}
export function runAutoMatch(initial:HostState,maxCommands=10000):HostState {let h=initial;for(let i=0;i<maxCommands;i++){if(h.status==='complete'||h.status==='guarded')return h;h=autoStep(h);}throw Error('Host command guard exceeded');}
