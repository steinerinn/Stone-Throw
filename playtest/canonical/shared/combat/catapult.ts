import type {ResolutionContext,SourceMetadata,ReactionEntry,Operation} from './contracts.js';import type {Cell,PlayerId} from '../model.js';import {seat,unitAt,available,parseKey,cellKey,boardSize} from './access.js';import {resolveImpact} from './impact.js';import {neighbors8} from '../rules/coordinates.js';import {random} from './rng.js';import {pushFrame,work} from './scheduling.js';import {requireDecision} from './decisions.js';
function finish(ctx:ResolutionContext,generated:ReactionEntry[]){const operations:Operation[]=[{kind:'hero-queue'},...generated.map(entry=>({kind:'wave',entries:[entry],terminalCheck:true} as Operation)),{kind:'terminal-check',reason:'catapult-sibling'}];pushFrame(ctx,'catapult',operations);}
export function catapultImpact(ctx:ResolutionContext,meta:SourceMetadata,cell:Cell,impact:number,generated:ReactionEntry[]){const p=seat(ctx.state,meta.targetPlayerId),k=cellKey(cell);if(p.shots.includes(k)){finish(ctx,generated);return;}const type=unitAt(ctx.state,meta.targetBoardId,k)?.type,result=resolveImpact(ctx,meta,cell);if(result.reaction)generated.push(result.reaction);if(type==='castle'||impact===4){finish(ctx,generated);return;}const next=neighbors8({size:boardSize(ctx.state,meta.targetBoardId)},cell.x,cell.y).filter(k=>!p.shots.includes(k));if(!next.length){finish(ctx,generated);return;}if(seat(ctx.state,meta.ownerId).decisionMode==='interactive'){const dest=next[Math.floor(random(ctx.rng,'catapult-roll')*next.length)]!;ctx.frames.at(-1)!.current.splice(ctx.frames.at(-1)!.cursor,0,work(ctx,{kind:'catapult',meta,cell:parseKey(dest),impact:impact+1,generated}));}else{const d=requireDecision(ctx,'catapult-roll',meta.ownerId,meta.targetBoardId,meta.sourceUnitId,next.map(parseKey));ctx.frames.at(-1)!.current.splice(ctx.frames.at(-1)!.cursor,0,work(ctx,{kind:'catapult-resume',meta,impact:impact+1,generated,decisionId:d.id}));}}
export function catapultSeries(ctx:ResolutionContext,ownerId:PlayerId,remaining:number){if(remaining<=0)return;const p=seat(ctx.state,ownerId),target=p.reactionTarget,cells=available(ctx.state,target.boardId,target.playerId);if(!cells.length)return;const meta:SourceMetadata={actorId:ownerId,ownerId,targetPlayerId:target.playerId,targetBoardId:target.boardId,sourceUnitId:null,source:'catapult-shot',origin:null};const d=requireDecision(ctx,'catapult-target',ownerId,target.boardId,null,cells.map(parseKey));const frame=ctx.frames.at(-1)!;frame.current.splice(frame.cursor,0,work(ctx,{kind:'catapult-resume',meta,impact:0,generated:[],decisionId:d.id}),work(ctx,{kind:'catapult-series',ownerId,remaining:remaining-1}));}
