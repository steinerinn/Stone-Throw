import type {ExplicitRng} from '../contracts.js';import {shuffle} from '../rng.js';
export function goblinTargets(available:readonly string[],rng:ExplicitRng,pacing=false):string[]{const candidates=[...available],count=Math.min(candidates.length,pacing?Math.min(15,Math.max(5,Math.ceil(candidates.length*0.066))):Math.max(5,Math.ceil(candidates.length*0.044)));shuffle(candidates,rng,'goblin-shuffle');return candidates.slice(0,count);}
