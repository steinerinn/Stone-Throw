import type { HostState } from '../host/contracts.js';
import type { NormalMemory } from './normal-policy.js';
/** Separate audit: legacy's synchronous result-line draw, not a new entropy
 * exception. The presentation receives only the selected existing prose. */
export declare function reserveResultMessage(host: HostState, memory: NormalMemory): void;
