import type { ResolutionContext, SourceMetadata, ReactionEntry } from '../contracts.js';
export declare function monkProximity(ctx: ResolutionContext, meta: SourceMetadata, k: string): ReactionEntry | null;
