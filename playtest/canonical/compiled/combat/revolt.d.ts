import type { ResolutionContext } from './contracts.js';
/** Reserve all targets before any impact. Never inspect hidden occupancy. */
export declare function beginRevoltPulse(ctx: ResolutionContext, level: number): void;
