import type { HostState } from '../host/contracts.js';
/** Random-placement policy proposes; the unchanged Stage 7 legality gate accepts. */
export declare function randomizePlacements(input: HostState): HostState;
