export const DISCONNECT_GRACE_MS=60*1000;
export const HEARTBEAT_TIMEOUT_MS=7000;
export function presence(room,now){const active=!room.closed&&room.host.status!=='complete';return room.seats.map(s=>{if(!s||s.left)return {connected:false,remainingMs:null,kickAllowed:false};if(s.controller==='ai')return {connected:true,remainingMs:null,kickAllowed:false};const connected=!s.absence&&now-s.seen<HEARTBEAT_TIMEOUT_MS;if(connected)return {connected:true,remainingMs:null,kickAllowed:false};if(!active)return {connected:false,remainingMs:null,kickAllowed:false};const remainingMs=(s.absence?.startedAt??s.seen+HEARTBEAT_TIMEOUT_MS)+DISCONNECT_GRACE_MS-now;return {connected:false,remainingMs,kickAllowed:s.absence?.policy==='wait',episode:s.absence?.id,policy:s.absence?.policy||'choice'};});}
