import type {Cell} from '../../model.js';import type {ExplicitRng} from '../contracts.js';import {random,shuffle} from '../rng.js';import {key,inBounds} from '../../rules/coordinates.js';
export const ARCHER_WEIGHTS=Object.freeze([5,15,40,15,7,6,5,4,2,1]);
export function archerShotCount(rng:ExplicitRng):number {const total=ARCHER_WEIGHTS.reduce((a,b)=>a+b,0);let r=random(rng,'archer-count')*total;for(let shots=0;shots<ARCHER_WEIGHTS.length;shots++){r-=ARCHER_WEIGHTS[shots]!;if(r<0)return shots;}return 9;}
export function archerTargets(size:number,origin:Cell,shots:ReadonlySet<string>,count:number,rng:ExplicitRng):Cell[]{const targets:Cell[]=[];for(let radius=1;radius<size&&targets.length<count;radius++){const ring:Cell[]=[];for(let dy=-radius;dy<=radius;dy++)for(let dx=-radius;dx<=radius;dx++){if(radius>1&&Math.max(Math.abs(dx),Math.abs(dy))!==radius)continue;const x=origin.x+dx,y=origin.y+dy;if(!inBounds({size},x,y)||shots.has(key(x,y)))continue;ring.push({x,y});}shuffle(ring,rng,'archer-ring');for(const target of ring){if(targets.length>=count)break;targets.push(target);}}return targets;}
